

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# JWT_SECRET: Used to sign and verify authentication tokens (see server/middleware/auth.ts).
# Set this to any long, random string. Do not commit your real value — this file is just a template.
JWT_SECRET="replace-with-a-long-random-string"

# MONGO_URI: Optional. If set, the app connects to this real MongoDB instance instead of
# spinning up an in-memory MongoDB (see server.ts -> connectDb()). Leave unset for local dev/testing.
MONGO_URI=""
